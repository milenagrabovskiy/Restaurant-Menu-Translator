services:

  db:
    image: postgres:16

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
      POSTGRES_DB: restaurant_menu_db

    volumes:
      - db-data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d restaurant_menu_db"]
      interval: 5s
      timeout: 3s
      retries: 5


  api:
    build: .

    env_file:
      - .env

    environment:
      DATABASE_URL: postgresql://postgres:root@db:5432/restaurant_menu_db

    ports:
      - "5000:5000"

    depends_on:
      db:
        condition: service_healthy


volumes:
  db-data: